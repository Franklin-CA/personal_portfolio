import 'package:flutter/material.dart';

/// Central colour tokens used across the portfolio.
class Palette {
  Palette._();

  // Light
  static const Color lightBg = Color(0xFFF5F4F1);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightInk = Color(0xFF14140F);
  static const Color lightMuted = Color(0xFF5E5C57);
  static const Color lightRule = Color(0xFFE0DDD6);
  static const Color accent = Color(0xFF2A4A6B);
  static const Color accentLight = Color(0xFF4A7AB5);

  // Dark
  static const Color darkBg = Color(0xFF0E0E0D);
  static const Color darkSurface = Color(0xFF1A1A18);
  static const Color darkInk = Color(0xFFECEBE7);
  static const Color darkMuted = Color(0xFFA8A6A0);
  static const Color darkRule = Color(0xFF2E2D2A);
}
